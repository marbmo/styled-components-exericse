const { allBeers, singleBeer, randomBeer, searchBeer } = require('./read');
const createBeer = require('./create');

module.exports = {
  allBeers,
  singleBeer,
  randomBeer,
  searchBeer,
  createBeer
}